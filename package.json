{
  "name": "edge-runtime",
  "version": "1.0.0",
  "description": "Edge function runtime and serverless framework with local dev server, KV store, and deploy tooling",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "edge": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "ts-node src/index.ts dev",
    "test": "vitest run",
    "lint": "eslint src/",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["edge", "serverless", "runtime", "functions", "kv-store", "deploy"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^11.1.0",
    "chokidar": "^3.5.3",
    "chalk": "^4.1.2",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "vitest": "^2.0.0"
  }
}
